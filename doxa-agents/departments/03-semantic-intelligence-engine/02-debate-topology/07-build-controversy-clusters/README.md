# build-controversy-clusters

Builds multi-sided controversy clusters from opposed/competing agreement-cluster relationships with lineage.

| Deploy | Output |
|--------|--------|
| `build_controversy_clusters` | `controversy_clusters`, `controversy_cluster_lineage` |

Deploy with `--no-verify-jwt` when invoked internally.
