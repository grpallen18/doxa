# refresh-topology-candidates

Expires stale position/cluster pair candidates when claims gain new neighbors.

| Deploy | Notes |
|--------|--------|
| `refresh_topology_candidates` | Replaces `refresh_claim_eligibility`; daily cron |
