# receive-scraped-content

Cloudflare Worker callback that stores raw scraped HTML for a story.

| Deploy | Notes |
|--------|--------|
| `receive_scraped_content` | Deploy with `--no-verify-jwt` (Bearer `SCRAPE_SECRET`) |

Next: [clean-scraped-content](../05-clean-scraped-content/).
