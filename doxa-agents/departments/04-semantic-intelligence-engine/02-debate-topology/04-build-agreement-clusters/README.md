# build-agreement-clusters

Build agreement clusters from classified position relationships (core union + soft attachments).

| Deploy | Input | Output |
|--------|-------|--------|
| `build_agreement_clusters` | `position_relationships` | `agreement_clusters`, `agreement_cluster_positions` |

Upstream: [classify-position-relationships](../03-classify-position-relationships/). Downstream: [generate-agreement-cluster-candidates](../05-generate-agreement-cluster-candidates/).
