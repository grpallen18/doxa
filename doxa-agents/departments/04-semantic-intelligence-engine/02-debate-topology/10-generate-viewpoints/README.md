# generate-viewpoints

Generates viewpoint narratives for controversy clusters.

| Deploy | Notes |
|--------|--------|
| `generate_viewpoints` | Cron in [schedules.sql](../schedules.sql); deploy with `--no-verify-jwt` |
