# discord-daily-health

Operational reporting for the ingestion and processing pipeline via Discord.

| Deploy | Schedule |
|--------|----------|
| `discord_daily_health` | Daily Discord summary |

Uses `get_daily_health_report` RPC; see migration history under `supabase/migrations/*health_report*`.
