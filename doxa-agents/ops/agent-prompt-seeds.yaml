# Dev/bootstrap reference for agent_prompt_slots — production uses SQL migrations.
# npm run agents:validate warns when prompt_kind: llm steps lack an entry here.
# Claims/extraction seeds removed with Cleanup (legacy claims path deleted).

seeds: {}
