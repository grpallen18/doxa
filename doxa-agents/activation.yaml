# Manual pipeline activation — steps listed here show status: active in the generated catalog.
# This does NOT schedule pg_cron jobs; run each step's schedule.sql in Supabase when ready.
#
# You remain responsible for:
#   1. Secret values (Supabase / Cloudflare dashboards)
#   2. Running schedule.sql in Supabase SQL Editor
#   3. supabase functions deploy
#   4. supabase/migrations
#   5. This file (which workflows are "live" in docs)
#
# Everything else (manifest.yaml, docs/generated/*) is auto-synced from code.
#
# L3 overhaul Session 5: Question-first six-step debate chain active; debate-pipeline cron in schedule.sql.

active:
  - ingest-newsapi
  - scrape-story-content
  - generate-evidence-check-candidates
  - run-evidence-checks
  - extract-citations
  - run-controversy-assessments
  - update-held-by-tracks
  - link-derived-media-clips
  - project-analysis-summaries
  - project-person-profiles
  - analysis-pipeline
  - retrieve-or-mint-questions
  - assign-question-answers
  - qualify-controversies
  - build-viewpoints
  - detect-disputes
  - project-debate-summaries
  - debate-pipeline
